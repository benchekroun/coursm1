GXX=g++
CXXFLAGS = -O2 
EXEC=MNT_exe
SRC= $(wildcard *.cpp)
OBJ= $(SRC:.cpp=.o)

LDLIBS   = -lglut -lGL -lGLU -lm -lpng


.PHONY:	run	clean

all: $(EXEC)

$(EXEC):$(OBJ) 
	$(GXX) $(LDFLAGS) -Wall -o $@ $^ glew.o $(LDLIBS)

%.o: %.cpp
	$(GXX)    -o $@    -c $< $(LDLIBS)

lib:
	$(GXX)    -o glew.o -c glew.c -DGLEW_BUILD -I./ -DGLEW_STATIC

run:	$(EXEC)
	./$(EXEC)

clean: 
	rm *.o *~ *# *.ppm


cleanall:
	rm *.o *~ *# ./$(EXEC)
